# TCP/IP From Scratch

Building the entire TCP/IP stack in Rust, layer by layer, from first principles.

## Layers
- [ ] physical   — unreliable wire simulation
- [ ] datalink   — framing + error detection  
- [ ] network    — IP addressing + routing
- [ ] transport  — UDP + TCP
- [ ] application — HTTP + DNS
